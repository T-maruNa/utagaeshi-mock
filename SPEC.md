# SPEC.md

# うたがえし mock仕様

## サービス名

うたがえし

## コンセプト

昔のうたに、今の言葉で返す。

## サービス概要

うたがえしは、毎日1首の古典短歌を原文のままお題として提示し、その短歌に対してユーザーが今の自分の言葉で「返し」を投稿するサービス。

AIは現代語訳の正解を出すのではなく、短歌の場面・感情・ニュアンスをやさしく読み解く補助をする。

サービスの主役は、短歌原文とユーザーの返し。  
AIは補助であり、主役にしない。

## 体験方針

うたがえしは、投稿を強制するサービスではない。

まずは、古典短歌をきっかけに生まれた現代の言葉を読める場所として成立させる。

見るだけのユーザーがいてもよい。  
読んでいるうちに「自分も返してみたい」と思える設計にする。

## 最新の画面方針

Phase 1 の最新方針では、`index.html` は「返しタイムライン」でも「今日のうたページ」でもなく、  
「今日のお題歌を読み、このうたに届いた返しを本のページのようにめくり、最後に自分も返したくなるホーム」とする。

返しタイムラインは `posts.html` に分離する。

`mypage.html` はプロフィール管理画面ではなく、  
自分の返しを本の1ページのように見返す「自分の返し帳」とする。

## MVPの基本機能

- 今日の短歌を表示する
- AIによる読み解きのヒントを表示する
- このうたに届いた返しを表示する
- うたがえしAIの返しを、みんなの返し一覧に混ぜる
- ユーザーが自分の言葉で返せる
- 返しにリアクションできる
- 自分の返し帳を表示する
- 過去のうたを見られる
- 人気のうたを見られる
- 返しタイムラインを見られる
- 短歌そのものに「このうた好き」を送れる

## MVP本線から外すもの

以下はMVP主導線には置かない。

- トップページの「今日いちばん響いた返し」
- 「返しの例」セクション
- AIに読んでもらう
- 人気ユーザーランキング
- DM
- コメント
- 外部SNSリンク
- フォロー機能

`ranking.html` は残してもよいが、MVP主導線ではなく実験的・補助的ページとして扱う。

## AIの役割

AIは「正解の現代語訳」を出さない。

AIの役割は以下。

- 短歌の場面をやさしく説明する
- 感情のニュアンスを補助する
- ユーザーが自分の言葉を出しやすくする
- 場を温める返しを、うたがえしAIとして出す

AI欄の見出し例：

```txt
AIによる読み解きのヒント
※正解ではありません
```

## index.html のページ仕様

index.html は、今日のお題歌を読み、このうたに届いた返しを本のページのようにめくり、  
最後に自分も返したくなるホーム。

トップページの主役は、めくるカード体験。  
返しを縦一覧で並べることは主役にしない。

初期表示は、今日のお題歌カード。

カード構成：

1. 今日のお題歌
2. このうたに届いた返し
3. このうたに届いた返し
4. うたがえしAIの返し
5. このうたに届いた返し
6. あなたなら、どう返しますか？

### 表示するもの

- 今日のお題歌カード（日付・短歌原文・作者・出典・このうた好き数・返し数・届いた反応数・このうた好きボタン・AI読み解きのヒント）
- このうたに届いた返しカード
- うたがえしAIの返しカード
- 最後の投稿導線カード
- 下部の控えめな投稿導線
- このうたへの返しを全部見る導線
- 明日ひらくうたの予告カード
- 昨日のうたを見る導線
- 過去のうたを見る導線
- 返しタイムラインへの導線

### 表示しないもの

- 返しの縦一覧を主役にしたセクション
- 今日いちばん響いた返し／1位／トップ／勝者／ランキング／人気ユーザー
- 昨日・今日・明日の横スライダー
- 明日のうたをめくるカード内に入れること
- 投稿しないと読めない設計

### めくり仕様

- 左右スワイプでカードを切り替える
- 初期表示は 1 / 6 の今日のお題歌カード
- 左スワイプで次のカードへ進む、右スワイプで前のカードへ戻る
- 1 / 6 では前へ戻れず、6 / 6 では次へ進めない（ループしない）
- 前後ボタンは補助として置いてよい
- カード切り替え時に軽いページめくり感を出してよい

### 投稿導線（2段階）

- 下部の控えめな投稿導線：カードをめくっている途中でも常時表示する補助導線。「今の自分なら、どう返しますか？　返す」
- 最後の投稿導線カード：主役。「あなたなら、どう返しますか？」＋補助文「正解じゃなくて大丈夫。今の自分ならどう言うかを書いてください。」＋ボタン「このうたに返す」

### AIの扱い（2種）

- AI読み解きのヒント：今日のお題歌カード内に控えめな折りたたみ導線として置く。初期状態は閉じる。「AI 読み解きのヒントを見る」
- うたがえしAIの返し：返しカード群の中に1枚だけ自然に混ぜる。「うたがえしAI」「AIによる返し」「※AIの返しです。正解や模範解答ではありません。」を明示。「返しの例」セクションは作らない。

### 明日ひらくうたの扱い

明日ひらくうたは、めくるカードには入れない。  
明日はまだ返せないため、今日のうたに届いた返しを読む流れと混ぜると混乱するため。

明日ひらくうたは、下部の小さな予告カードとして表示する。「明日ひらくうた／このうたは明日ひらきます。まだ返せません。」

### mypage.html との関係

index.html と mypage.html はどちらも本のページのようにめくる体験を持つが、役割は違う。

- index.html：今日のうたから始まり、他人の返しやAI返しを読んで、最後に自分も返す
- mypage.html：自分の過去の返しを1枚ずつ見返す
