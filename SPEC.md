# SPEC.md

# うたがえし mock仕様（景色ファースト）

## サービス名

うたがえし

## コンセプト

千年前の歌人と、同じ景色を見る。

## サービス概要

うたがえしは、毎日ひとつ「今日の景色」を出し、それを見て浮かんだことを、
今の自分の言葉で残すサービス。

同じ景色から生まれた言葉が1枚ずつめくれて、その中に**千年前の人の言葉**も混ざっている。
残した言葉は返し帳にたまり、あとから開くと、その日の景色まで一緒に思い出せる。

古典は「今日の教材」ではない。**同じ景色について何か残した千年前の一人**として置く。
歌番号・作者・出典は、言葉より前に出さない。

## 体験の流れ

```
今日の景色に出会う
→ なんとなく一言残したくなる
→ 自分の言葉が一枚になる
→ 同じ景色から出た他人の言葉をめくる
→ その中に千年前の人の言葉まで混ざっている
→ 後で返し帳を開くと、その日の景色まで思い出す
```

投稿を強制しない。読むだけでも成立する。

## 設計原則

1. **基本単位は visual-card（景色 × 言葉 の一枚）。** 共通部品ではなくサービスの最小単位で、全画面で同じものを使う
2. **1 scene = 1 image。** その日の景色に属するカード（自分／他人／千年前）はすべて同じ画像を共有する。これで「同じ景色を見た人たち」が視覚的に成立する
3. **主役は景色と言葉だけ。** 作者名・歌番号・統計は副情報として一段下げる（AIの読み解き解説は廃止）
4. **画面の役割を混ぜない。** 今日＝書く場所／返し帳＝思い出す場所／これまでの景色＝眺める場所
5. **人物情報は言葉より前に出さない。** カードでは弱く、一覧・詳細・返し帳では意味を持たせる
6. **同じ見た目のものは同じ触り方でめくれる。** デッキ・返し帳・はじめての方へで、めくる操作と目次の挙動を揃える

## MVPの基本機能

- 今日の景色を表示する
- 景色に対して、自分の言葉をひとつ残す（1日にひとつ）
- 同じ景色から生まれた言葉をめくって読む（自分・他の人・千年前）
- 言葉に「届いた」を送る
- 自分の言葉を返し帳で読み返す（その日の景色に切り替えられる）
- 心に残った他の人の言葉を、返し帳の別タブに残す
- これまでの景色を月ごとにたどる
- 他の人が残してきた言葉を読む
- 言葉をXへシェアする

## MVP本線から外すもの

- AIによる読み解きのヒント（**廃止**。解説はサービスの主役を教材側へ引き戻す）
- 全日横断の返しタイムライン（`/posts`）
- 人気のうた・ランキング・人気ユーザー
- 言葉ひとつぶんの詳細ページ（`/replies/[id]`）
- 「このうた好き」ボタン
- DM・コメント・フォロー
- 外部SNSへの自分のプロフィールリンク（フォロワー稼ぎ目的の導線）
- 通知設定・プライバシー設定などの空の設定枠

言葉を画像や本文でXへ送る「外部への宣伝」導線は、上の外部SNS制限には当たらない。

## 匿名ユーザーと登録

**即時価値は匿名で全部渡す。長期価値だけを登録理由にする。**

```
景色を見る → 匿名で一言残す → 自分のvisual-cardが出る
→ 他人＋千年前の言葉をめくる
                                  ここまで匿名で可能
```

- 投稿前に登録の壁を置かない
- 匿名で残した言葉は、登録しても**引き継がない**。だから匿名のあいだ、自分のカードの下段右は
  「返し帳に残しました」ではなく「今日の景色に残しました」と書く
- 匿名で残せるのは1日10人ぶんまで。枠が尽きたとき・この端末で今日すでに残したときは、
  入力欄を灰色にして叱るのではなく、欄ごと差し替えて、起きたことと次にできることだけを静かに置く
- 残り枠は3人ぶんを切ってからだけ知らせる（常に出すと急かして見える）
- 匿名ユーザーが返し帳を開いたときは、空の本ではなく**返し帳の価値を説明する登録導線**を出す

## 画面仕様

### index.html（今日）

- **目的**：景色に出会い、軽く一言残す。
- **主役**：今日の景色の visual-card。
- **並び**：デッキ → `← n / m →` → 投稿欄（未投稿のときだけ）→ みんなの言葉（2列グリッド）→ これまでの景色／返し帳への出口。
- **デッキの構成**：

  ```
  1ページ目    今日の景色（常に固定）
  2ページ目    自分（投稿済みなら必ずここ）
  3ページ目〜  千年前1 + 他の人 をランダム順
  ```

  言葉は最大8枚（自分・千年前を含む）。景色を足してデッキは最大9ページ。
  **開いた時に一度だけ抽選**し、←→や目次では変わらない。リロードで変わってよい。
- **投稿の前後で画面の作りを変えない。** デッキに自分の言葉が1枚増え、投稿欄が消えるだけ。
  URLも変えない（今日のページの一貫性が切れるため）。
- **2列グリッドはデッキの目次。** デッキの2ページ目以降と同じ並び・同じ枚数を並べ、
  押してもページ遷移せず、上のカードが切り替わる。景色を見ている間はどの札も光らない。
- **置かないもの**：日付の移動（これまでの景色へ移管）、AI解説、タグ、「書かずに見る」ための別画面、
  「今日、残った言葉」の縦一覧。

### day.html（その日の景色）

- **目的**：古典を振り返るのではなく、**その日の自分を拾い直す**。
- 自分の投稿がある日：景色 → **あなたは、こう残しました** → その日に残った言葉（他人・千年前）。
- 自分の投稿がない日：景色 → その日残った言葉 → 千年前の言葉。
- ホームとの違いは3つだけ：投稿欄が無い／並びを抽選しない／上限が無い（その日の全部を並べる）。
- 自分の言葉を開いているときだけ、カード下の「…」に整える・削除を畳む。

### scenes.html（これまでの景色）

- **目的**：ひらいた日をたどる。日付の移動の役割をここに集約する。
- 1日1行。**主役はその日の景色の文**で、写真は56pxのサムネイルに留める。
  画像は1か月8枚なので、大きく格子に並べると同じ絵が月に4回ずつ出て、日を見分けられない。
- 月ごとの折りたたみ（今月は開いたまま・過去月は畳む）。
- 景色が無い日は並べない（歯抜けを空カードで埋めない）。

### mypage.html（返し帳）

**投稿履歴を見る場所ではない。過去の自分の言葉から、その日の景色や周辺の記憶を思い出す場所。**

- 大きな visual-card を1枚ずつ表示する「本」形式
- `前のページ / 次のページ`と左右スワイプで移動
- 月ごとの目次（今月は展開、前月以前は折りたたみ）。**行を押しても遷移しない**
- `残した言葉` / `心に残った言葉` の2タブ
- カード内で **自分の言葉 ⇄ この日の景色** を切り替えられる
- **他のページへ遷移しない**（遷移で回想を中断させない）
- 未投稿日は表示しない。「この日は残しませんでした」で歯抜けを埋めない
- 連続日数などの達成指標は前に出さない（残せなかった日を責めない）
- **ページ番号は古い順に固定**（あとから投稿しても既存の番号が変わらない）。
  **目次は新しい日付から降順**。付与順と表示順が逆なので取り違えないこと
- 1件のときはページャを出さない（移動先が無い）

**返し帳に千年前の言葉は混ぜない（意図的な非対称）。** ホームのデッキでは自分／他人／千年前が
同じ景色から生まれた言葉として混ざるが、返し帳は自分が過ごした日の記憶を拾い直す場所なので、
`残した言葉`＝自分の言葉だけ、`心に残った言葉`＝自分が反応して保存した他人の言葉。

### user.html（○○さんの残した言葉）

- **「返し帳」と呼ばない。** 返し帳は個人的な記憶のイメージなので、自分＝返し帳／
  他人から見たら＝この人の言葉、と呼び分ける。
- ここではプロフィール（アイコン・表示名・自己紹介）を前に出してよい。
- デッキ＋目次（ホーム・その日の景色と同じ形）。
- **デッキは5枚から始める**（全部積むと、100件残した人が100ページのデッキになる）。
  最初の5枚は「届いた」の多い順。**目次はぜんぶ載せ**、そこから開いたものはデッキの末尾に足す。
- 順位で選ぶのはデッキの5枚だけ。並べ替えた一覧は作らない（目次は日付順のまま）。
- フォロー・DM・ランキングは持たない。

### profile.html（プロフィール）

- **自分が他人からどう見えるかを確認・編集する場所。**
- アイコン／表示名／自己紹介。編集は**この画面の中で**行う（別ページへ飛ばさない）。
- `残した言葉 ○件` / `心に残った言葉 ○件` は飾りにせず、押すと返し帳の該当タブが開く。
- 自分の公開ページ（`user.html`）への導線を持つ。

### account.html（アカウント設定）

**アカウントそのものを管理する場所に限定する。** 返し帳・投稿数・プロフィールは持ち込まない。

- メールアドレスの表示と変更（本人確認にいまのパスワードを訊く／確認メールは新しいアドレスにだけ送る）
- パスワードの再設定（この画面では入力させず、登録のアドレスへリンクを送る）
- ログアウト
- 退会
- 空の設定枠や「準備中」の項目は置かない

### intro.html（はじめての方へ）

- 1ページ1メッセージの「小さな本」。全4ページ。
- **ここだけ visual-card を使わない。** 実物のカードで作り直したこともあったが、
  めくっても同じ画面に見えて意味が通らなかったため戻した。
- 左右スワイプ＋前後ボタン。ループしない。
- 初回訪問者をここへ送る導線は持たない（ホームから始めてよい）。

## ナビゲーション

```
BottomNav     ホーム / これまで / 返し帳
Header menu   プロフィール / アカウント設定 / 言葉を整える・削除する / はじめての方へ / ログアウト
```

頻繁に行く場所はBottomNav、管理系はメニュー。
ハンバーガーに全部詰めると、長期価値である「過去の返し帳を見る」が1タップ遠くなる。

## 言葉の長さとカードの寸法

- 言葉の上限は140字
- カードは長い言葉が来ても高さを変えない。入らなければ文字を1段ずつ小さくし、
  最小でも入らなければ省略して「続きを読む」を出す（その場で開く）
- 4段階（28 / 23 / 19 / 17px）は決定済み。開始サイズは種類ごとに違う

  | 種類 | 開始サイズ |
  | --- | --- |
  | 自分の言葉・他の人の言葉 | xl（28px） |
  | 景色 | lg（23px）＝言葉より一段控えめ |
  | 千年前のうた | md（19px）＝五句が収まる段階 |

## 景色の文と画像

- 景色の文は3〜4行。1行は2〜14文字、句点は0〜2個。数字・疑問符は使わない
- 画像は日次生成せず、事前制作した月ごと8枚（通常7＋fallback 1）のライブラリから選ぶ
- 選んだ画像はその日の景色に保存する。あとから画像を足しても**過去の日の背景は変えない**
  （変えると返し帳の記憶が壊れる）
- visual-card ではほぼ1:1に切るので、主要被写体は中央正方形に収まる構図。
  中央〜左は文字が載るので情報量を抑える
